# スクリプトでテスト実行時に読み込み権限をなくす
http {
    server {
        listen 80;
        server_name "server1";
        location / {
            root /data/server1/;
            index index.html index.htm;
        }
    }
}
